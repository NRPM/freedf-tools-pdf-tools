/* ============================================================
   intelligence.js — REMOVED (AI Summarizer + Translate PDF)
   These tools were removed from the app per user request.
   The file is kept as an empty module so app.js's
   `register(window.ToolIntelligence || {})` stays safe.
   ============================================================ */
(function () {
  'use strict';
  window.ToolIntelligence = {};
})();
